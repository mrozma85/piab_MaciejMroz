@model IEnumerable<Projekt.Data.Data.Oferta.RodzajTransportu>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Kod)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nazwa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cena)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdOferty)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PromocjaOferty)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Kod)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cena)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdOferty)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PromocjaOferty)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRodzajuTransportu">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRodzajuTransportu">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRodzajuTransportu">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
